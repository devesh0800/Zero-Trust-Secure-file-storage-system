{
  "name": "secure-file-storage-workspace",
  "version": "1.0.0",
  "description": "Root workspace to run frontend and backend concurrently",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix secure-file-storage\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix secure-file-storage && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
